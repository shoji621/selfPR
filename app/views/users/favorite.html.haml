.contents
  .content__title
    = @user.name
    さん
  .content__follow{id: "followbox-#{@user.id}"}
    = render partial: "shared/followbox", locals: { user: @user }

  %ul.menu
    %li
      = link_to user_path(@user.id) do
        #posts.menu_item 投稿一覧
    %li
      #favorite_posts.menu_item.active お気に入り投稿一覧
  .container
    .show
      - @favorite_posts.each do |post|
        = render partial: "shared/post", locals: { post: post }
      = paginate(@favorite_posts)